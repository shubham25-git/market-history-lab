services:
  - type: web
    name: market-history-lab
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /var/data
    disk:
      name: market-history-lab-data
      mountPath: /var/data
      sizeGB: 1
